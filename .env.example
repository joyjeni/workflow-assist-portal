# PostgreSQL connection string (Vercel Postgres, Neon, Supabase, or local)
DATABASE_URL="postgresql://postgres:postgres@localhost:5432/workflow_assist_portal?schema=public"

# Auth secret used to sign session JWTs (any long random string)
AUTH_SECRET="replace-with-a-long-random-string-at-least-32-chars"

# Public app URL (optional)
NEXT_PUBLIC_APP_URL="http://localhost:3000"
